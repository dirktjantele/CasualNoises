
Patch in port.c line 754				ToDo: find out why this solves a semaphore realted issue

			uint8_t prio = 0x50;
			if ( ucMaxSysCallPriority != prio )
				ucMaxSysCallPriority = prio;
			configASSERT( ucCurrentPriority >= ucMaxSysCallPriority );



Device allocation:
------------------

ADC1 1V/OCT & CV inputs
ADC2 1V/OCT inputs

SPI2 NerveNet North Side <-> South Side

TIM2 ADC3 trigger (10 KHz)
TIM3 Internal clock (when external clock in is not used) (200Hz)
TIM5 Used to calculate elapsed time during processBlock::AudioProcessor() + overhead (1MHz)